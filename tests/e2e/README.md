# End-to-End Test Purpose

This directory is reserved for future user-visible triadic workflow testing.

Expected future coverage includes user checkpoint, steering, pause/resume, deliberation and publication flows.

Phase 4 is expected to introduce real E2E coverage.

No Playwright dependency, configuration or specification is introduced in Phase 0.

E2E tests must not call production model providers by default.
